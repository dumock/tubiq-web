{
  "name": "tubiq-monorepo",
  "private": true,
  "workspaces": [
    "collector-worker",
    "crawler-worker"
  ],
  "scripts": {
    "start:collector": "npm --workspace collector-worker run start",
    "start:crawler": "npm --workspace crawler-worker run start"
  }
}
